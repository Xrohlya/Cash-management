const tg=window.Telegram?.WebApp;if(tg){tg.ready();tg.expand();}const $=id=>document.getElementById(id);const money=n=>Number(n||0).toLocaleString('ru-RU',{maximumFractionDigits:0})+' ₽';function send(payload){if(tg){tg.sendData(JSON.stringify(payload));$('hint').textContent='Отправлено боту.'}else $('hint').textContent='Откройте Mini App из Telegram.'}document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>send({type:b.dataset.action}));$('sendExpense').onclick=()=>{const amount=parseFloat(($('amount').value||'').replace(',','.'));const description=$('description').value.trim();if(!amount||amount<=0){$('hint').textContent='Введите сумму.';return}send({type:'expense',amount,description})};$('closeBtn').onclick=()=>tg?.close();if(tg?.initDataUnsafe?.user)$('hint').textContent='Подключено: '+(tg.initDataUnsafe.user.first_name||'пользователь');